import { FC } from 'react';

const Game: FC = () => {
    // Create the main game algorithm
    // 1. Try to play some music on navigator
    // 2. Get some data from Spotify API to define which music
    //    is a hard one
    // 3. Get chorus from a Spotify music (or define which part
    //    is the most listen for difficulty).
    return <div>GAME</div>;
};

export default Game;
